apiVersion: apps/v1
kind: Deployment
metadata:
  name: ram-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ram
  template:
    metadata:
      labels:
        app: ram
    spec:
      containers:
      - name: ram-container
        image: your-dockerhub-username/ram-image:latest
        ports:
        - containerPort: 80
